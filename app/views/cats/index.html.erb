<div>
  <h1>Cats</h1>

  <% unless @cats.empty? %>
    <ul>
      <% @cats.each do |cat| %>
        <li><%= link_to cat.name, cat_url(cat) %></li>
      <% end %>
    </ul>
  <% end %>

  <%= link_to "Create a new cat", new_cat_url %>
</div>
