<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapsed">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_url %>">
        <img alt="99kittens" src="<%= image_url("99kittens.png") %>" width=20%>
      </a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapsed">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.nil? %>
          <li><%= link_to "Log in", new_session_url %></li>
          <li><%= link_to "Register", new_user_url %></li>
        <% else %>
          <li><%= link_to "Log out", session_url, method: :delete %></li>
        <% end %>
      </ul>
    </div>


  </div>
</nav>
